# app

Next.js App Router pages, route handlers, and shared UI components.

- `layout.tsx` wires the global shell.
- `page.tsx` is the home route.
- Feature routes live under `intro/`, `projects/`, `blog/`, `life/`, `friends/`, and `settings/`.
